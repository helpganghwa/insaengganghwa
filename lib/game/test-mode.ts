/**
 * 실운영 전 테스트 기간 플래그.
 *
 * 테스트 기간 동안 ×5로 지급되는 보상: 출석체크 · 일일 우편 보급.
 * - 출석체크·일일 우편은 아래 배율 상수로 적용(false 시 ×1 자동 원복).
 * - 신규가입 보너스는 DB 트리거(0007: 다이아 10000 / 상자 100)가 별도 적용 — 이 상수와
 *   연동 안 됨. 해제/조정 시 SQL 재적용 필요(0006=기본값 1000/10).
 */
export const TEST_MODE = true;

/** 테스트 기간 보상 배율 — 출석체크·일일 우편에 적용. */
export const TEST_REWARD_MULTIPLIER = TEST_MODE ? 5 : 1;
