-- 0200: 아바타 관리 화면 순서 편집 (2026-09-15)
--
-- 문의 "아바타가 늘어나니 원하는 순서대로 배치하고 싶다". 관리 화면(/me/profiles)의 보유 목록은
-- 생성 최신순 고정이었다(활성 접속자 492명 중 3개 이상 163명, 최대 23개). 유저가 정한 순서를 담는
-- 정렬값을 둔다. 표시 순서는 (sort_order asc, created_at desc).
--
-- 0 = "아직 배치되지 않음"(기본값). 새 아바타는 항상 0으로 들어가 맨 앞에 놓이고(같은 0끼리는
-- 최신순), 유저가 순서를 저장하면 그 시점의 전체 목록이 1..N으로 다시 매겨진다. 그래서 기존 행은
-- 백필 없이(전부 0) 지금 보이는 순서 그대로 시작하고, 생성 경로 3곳(pipeline·어드민·기본 아바타)은
-- 손대지 않는다. 관리 화면 전용 — 파견 배정 등 다른 목록은 생성 최신순 유지(2026-09-15 사용자 결정).
alter table user_profiles add column if not exists sort_order integer not null default 0;
